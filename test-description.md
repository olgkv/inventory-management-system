## 1. Общая информация

Требования к процессу:

- выполнить практическое задание;
- если что-то непонятно, описать вопросы и предположения (например, в комментариях в коде);
- по завершении:
  - выложить код на GitHub;
  - отправить ссылку рекрутеру;
  - добавить инструкцию по запуску в `README.md`.

---

## 2. Формулировка задания

Нужно создать **простое приложение для учета товаров** со следующим функционалом:

1. базовый функционал:

- добавление товаров;
- просмотр товаров;
- удаление товаров;

2. разделение на страницы:

- не более **50 товаров на одну страницу**;

3. минималистичный дизайн:

- использование **Tailwind CSS**.

---

## 3. Требования к стеку

### Backend

- Node.js **18+**
- TypeORM
- PostgreSQL

### Frontend

- React (**Vite**)
- TypeScript
- Tailwind CSS

---

## 4. Требования к Backend части

### 4.1. Сущность Product

```ts
{
  id: number; // автоинкремент
  article: string; // артикул (уникальный)
  name: string; // название товара (обязательное)
  price: number; // цена (обязательное, >0)
  quantity: number; // количество на складе (>=0)
  createdAt: Date; // дата создания
}
```

---

### 4.2. API Endpoints

#### GET /products

- список товаров с пагинацией;
- параметры: `page`, `limit`;

Формат ответа:

```json
{
  "data": [...], // товары на странице
  "total": 50 // общее количество товаров
}
```

#### POST /products

- создание товара;
- поля:
  - article
  - name
  - price
  - quantity

#### PUT /products/:id

- обновление товара

#### DELETE /products/:id

- удаление товара

---

### 4.3. Валидация (Backend)

- `article`:
  - не повторяется;

- `name`:
  - не пустое;

- `price`:
  - число больше 0;

- `quantity`:
  - число больше или равно 0.

---

## 5. Требования к Frontend части

### 5.1. Главная страница

Должна содержать:

- таблицу товаров со столбцами:
  - ID
  - Название
  - Артикул
  - Цена
  - Количество;

- постраничную навигацию:
  - кнопка «Назад»;
  - кнопка «Вперед»;
  - номер текущей страницы;
  - общее количество страниц (между кнопками);

- кнопки:
  - редактирования товара;
  - удаления товара.

---

### 5.2. Форма добавления и редактирования

Поля формы:

- name (название);
- article (артикул);
- price (цена);
- quantity (количество).

Валидация:

- подсветка ошибок;
- вывод текста ошибки под соответствующим полем
  - пример: отрицательная цена.

---

### 5.3. UI

- минималистичный интерфейс;
- Tailwind CSS;
- дизайн на усмотрение исполнителя;
- таблица товаров должна выглядеть **примерно так**:

```
| ID | Название | Артикул | Цена | Количество | Действия |
|----|----------|---------|------|------------|----------|
| 1  | Хлеб     | NB-001  | 899  | 5          | [✏][🗑]  |
```

---

## 6. Требования к предоставлению решения

Нужно предоставить **полноценное Docker-решение**, включающее:

- backend с API;
- frontend с UI;
- PostgreSQL:
  - автоматически инициализируется при запуске;
  - содержит таблицы и тестовые данные;
- Docker Compose:
  - объединяет frontend, backend и БД.

Требования к запуску:

- запуск одной командой: `docker-compose up`

- данные БД должны **сохраняться между перезапусками контейнеров**
